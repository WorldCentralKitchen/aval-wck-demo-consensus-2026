/**
 * Operational scripts entrypoint marker.
 *
 * Phase 0b populates this directory with:
 *   - register-schemas.ts (registers all 4 EAS schemas, writes UIDs back)
 *   - create-wallets.ts   (generates Base Sepolia keypairs)
 *   - fund-status.ts      (checks faucet/treasury balances)
 */
export {};
