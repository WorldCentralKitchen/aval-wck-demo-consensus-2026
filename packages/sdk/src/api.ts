/**
 * Typed REST client — populated in Phase 1b alongside the API Gateway routes.
 *
 * Will export functions matching the seven Lambdas:
 *   - postEnroll, postRedeem, postSettle
 *   - postAttestIssue, getAttestVerify
 *   - postOnboardingReview
 *   - getCredentialVerify (the x402-paywalled one)
 */
export {};
