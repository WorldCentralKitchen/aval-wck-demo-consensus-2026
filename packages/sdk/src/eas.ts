/**
 * EAS helpers — populated in Phase 1a.
 *
 * Will export:
 *   - attest({ schemaUid, recipient, payload, revocable })
 *   - verifyLatest({ schemaUid, holder })
 *   - revoke({ uid })
 *   - attestVendor(walletAddress, region, category, validUntil)
 */
export {};
