/**
 * Coinbase Developer Platform helpers — populated in Phase 1a.
 *
 * Will export:
 *   - createVendorWallet()                        // for new vendor onboarding
 *   - disburseToVendor(walletAddress, usdcAmount) // treasury → vendor USDC transfer
 *   - getWalletBalance(address)
 */
export {};
